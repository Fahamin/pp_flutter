import 'package:flutter/material.dart';

class M3uList extends StatelessWidget {
  const M3uList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
